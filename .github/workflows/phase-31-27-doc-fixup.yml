name: Phase 31.27 documentation fixup

on:
  push:
    branches:
      - codex/phase-31-27-production-auth-decision
    paths:
      - .github/workflows/phase-31-27-doc-fixup.yml

permissions:
  contents: write

jobs:
  fixup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/phase-31-27-production-auth-decision
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Apply exact documentation fixup
        run: node scripts/phase-31-27-doc-fixup.mjs
      - name: Commit fixup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/architecture.md docs/implementation-preparation.md
          git diff --cached --check
          git diff --cached --quiet && { echo "No fixup to commit"; exit 1; }
          git commit -m "Fix Phase 31.27 source-of-truth contradictions"
          git push origin HEAD:codex/phase-31-27-production-auth-decision
