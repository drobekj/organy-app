name: Issue 233 guarded implementation patch

on:
  push:
    branches: [fix/issue-233-live-working-conflict]
    paths:
      - scripts/issue-233-patch.py
      - scripts/issue-233-compat.py
      - .github/workflows/issue-233-patch.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: organy_issue_233_patch
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d organy_issue_233_patch"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/organy_issue_233_patch
      ORGANY_RUNTIME: db
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/issue-233-live-working-conflict
      - name: Apply guarded product patch
        run: python scripts/issue-233-patch.py
      - name: Align legacy acceptance
        run: python scripts/issue-233-compat.py
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run db:migrate
      - name: Issue 224 regression
        run: npx tsx scripts/issue-224-tests.ts
      - name: Issue 225 regression
        run: npx tsx scripts/issue-225-tests.ts
      - name: Issue 230 regression
        run: npx tsx scripts/issue-230-tests.ts
      - name: Issue 233 acceptance
        run: npx tsx scripts/issue-233-tests.ts
      - run: npm run typecheck
      - run: npm test
      - name: Commit product patch and remove staging helpers
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app src scripts/issue-224-tests.ts scripts/issue-230-tests.ts scripts/issue-233-tests.ts .github/workflows/issue-233.yml
          git rm scripts/issue-233-patch.py scripts/issue-233-compat.py .github/workflows/issue-233-patch.yml
          git commit -m "Issue 233: fix alarm geometry and live Working conflicts"
          git push origin HEAD:fix/issue-233-live-working-conflict
