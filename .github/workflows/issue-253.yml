name: Issue 253 compact Audit History acceptance

on:
  pull_request:
    paths:
      - app/admin/audit-history/page.tsx
      - app/admin/accounts/page.tsx
      - app/layout.tsx
      - app/issue-253-audit-history.css
      - src/application/audit-history-view.ts
      - scripts/issue-253-tests.ts
      - package.json
      - .github/workflows/issue-253.yml
  workflow_dispatch:

jobs:
  issue-253:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund --loglevel=error
      - name: Issue 253 compact Audit History acceptance
        run: npm run test:issue-253
      - run: npm run typecheck
      - run: npm test
      - name: Production build
        run: npm run build
