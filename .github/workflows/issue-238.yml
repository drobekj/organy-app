name: Issue 238 Compact workspace UX

on:
  pull_request:
    paths:
      - app/protected-account-controls.tsx
      - app/issue-238-workspace.css
      - app/layout.tsx
      - app/planning-lifecycle-client.tsx
      - scripts/issue-238-tests.ts
      - package.json
      - package-lock.json
      - .github/workflows/issue-238.yml
  workflow_dispatch:

jobs:
  issue-238:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund --loglevel=error
      - name: Compact workspace acceptance
        run: npm run test:issue-238
      - run: npm run typecheck
      - run: npm test
      - name: Production build
        run: npm run build
