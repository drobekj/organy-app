# Phase 31.22 HUMAN checkpoint

Only two refinements are new.

Verify:

1. Open Antiphon by clicking its input; click the same Antiphon input again without selecting anything. The candidate list closes and the last confirmed Antiphon (or empty `Select antiphon`) is restored. A third click opens it again.
2. Repeat the same open → second-click close → third-click reopen sequence for Topic.
3. Empty Service note shows the muted prompt `Add service note…`.
4. Typing a Service note replaces the prompt with normal foreground text; the field remains full width and one normal input-control line high.

No previously accepted keyboard, Mixed-language, candidate signal, persistence or general layout behavior needs retesting.
