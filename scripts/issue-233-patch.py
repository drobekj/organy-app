from pathlib import Path


def replace_once(path: str, old: str, new: str) -> None:
    p = Path(path)
    text = p.read_text(encoding="utf-8")
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{path}: expected exactly one match, found {count}: {old[:120]!r}")
    p.write_text(text.replace(old, new, 1), encoding="utf-8")


# 1) Single native 3px conflict border: no secondary outline.
replace_once(
    "app/globals.css",
    ".saved-set-list button.needs-revision-record {\n  background: #fef3f2;\n  border-color: var(--danger);\n  outline: 3px solid var(--danger);\n  outline-offset: -3px;\n}",
    ".saved-set-list button.needs-revision-record {\n  background: #fef3f2;\n  border: 3px solid var(--danger);\n}",
)
replace_once(
    "app/globals.css",
    ".needs-revision-row {\n  background: #fef3f2;\n  border-color: var(--danger);\n  outline: 3px solid var(--danger);\n  outline-offset: -3px;\n}",
    ".needs-revision-row {\n  background: #fef3f2;\n  border: 3px solid var(--danger);\n}",
)

# 2) Pure UI resolver so immediate optimistic clearing and stale-preview behavior are testable.
Path("src/planning-lifecycle/conflict-ui.ts").write_text('''export type PlanningDraftConflictPreviewState = {\n  key: string;\n  conflictingRowIndexes: number[];\n};\n\nexport function resolvePlanningDraftConflictRow(input: {\n  persistedConflict: boolean;\n  persistedSongId?: string;\n  draftSongId?: string;\n  selectedCandidateSuppressedByMelodyWindow?: boolean;\n  currentPreviewKey: string;\n  preview?: PlanningDraftConflictPreviewState | null;\n  rowIndex: number;\n}): boolean {\n  if (input.preview && input.preview.key === input.currentPreviewKey) {\n    return input.preview.conflictingRowIndexes.includes(input.rowIndex);\n  }\n\n  if (input.draftSongId !== input.persistedSongId) {\n    return Boolean(input.draftSongId && input.selectedCandidateSuppressedByMelodyWindow);\n  }\n\n  return input.persistedConflict;\n}\n''', encoding="utf-8")

# 3) Expose a read-only authoritative preview of the current Working draft.
replace_once(
    "app/api/planning-lifecycle/route.ts",
    'import { enrichCompletedConflictStates, enrichPlanningConflictStates, enrichRevisionRowIndexes, previewCompletedPlanInvalidation } from "../../../src/application/completed-plan-conflict-preview";',
    'import { enrichCompletedConflictStates, enrichPlanningConflictStates, enrichRevisionRowIndexes, findCompletedPlanConflicts, previewCompletedPlanInvalidation } from "../../../src/application/completed-plan-conflict-preview";',
)
replace_once(
    "app/api/planning-lifecycle/route.ts",
    '  | "previewCompletedRecordInvalidation"\n  | "saveWorkingSet"',
    '  | "previewCompletedRecordInvalidation"\n  | "previewPlanningSetConflict"\n  | "saveWorkingSet"',
)
route_marker = '''    if (action === "previewCompletedRecordInvalidation") {\n      if (actor.role !== "admin") return NextResponse.json({ success: false, error: { code: "permissionDenied", message: "Only the active admin role can preview completed-plan invalidation." } });'''
route_insert = '''    if (action === "previewPlanningSetConflict") {\n      if (!isPlanningSetConflictPreviewInput(body.input)) return invalidInput("Planning conflict preview requires setId, serviceDate and rows.");\n      const planningRepository = new DrizzlePlanningSetRepository(adapterDependencies);\n      const currentSet = await planningRepository.findById(body.input.setId);\n      if (!currentSet) return NextResponse.json({ success: false, error: { code: "notFound", message: "Planning set was not found." } });\n      if (currentSet.status !== "working") return NextResponse.json({ success: false, error: { code: "invalidStatus", message: "Only a Working planning set can be previewed as an editable draft." } });\n      const completedRecords = await new DrizzleCompletedServiceRecordRepository(adapterDependencies).list();\n      const melodyWindow = await new PostgresNonRepetitionPeriodService(pool).get(actor);\n      const proposedPlan: PersistedPlanningSet = {\n        ...currentSet,\n        serviceContext: { ...currentSet.serviceContext, serviceDate: body.input.serviceDate },\n        rows: body.input.rows,\n      };\n      const impacts = await findCompletedPlanConflicts(\n        [proposedPlan],\n        completedRecords,\n        melodyClasses,\n        melodyWindow.success ? melodyWindow.value.months : 2,\n      );\n      const conflictingRowIndexes = [...new Set(impacts.flatMap((impact) => impact.conflictingRowIndexes))].sort((left, right) => left - right);\n      return NextResponse.json({ success: true, value: { conflictingRowIndexes } });\n    }\n\n    if (action === "previewCompletedRecordInvalidation") {\n      if (actor.role !== "admin") return NextResponse.json({ success: false, error: { code: "permissionDenied", message: "Only the active admin role can preview completed-plan invalidation." } });'''
replace_once("app/api/planning-lifecycle/route.ts", route_marker, route_insert)
replace_once(
    "app/api/planning-lifecycle/route.ts",
    '  return ["getWorkspaceSnapshot", "listPlanningSets", "listCompletedRecords", "loadPlanningSet", "loadCompletedRecord", "previewCompletedRecordInvalidation", "saveWorkingSet", "finalizeWorkingSet", "reopenFinalSet", "completeFinalSet", "deletePlanningSet", "updateCompletedRecord", "deleteCompletedRecord"].includes(action);',
    '  return ["getWorkspaceSnapshot", "listPlanningSets", "listCompletedRecords", "loadPlanningSet", "loadCompletedRecord", "previewCompletedRecordInvalidation", "previewPlanningSetConflict", "saveWorkingSet", "finalizeWorkingSet", "reopenFinalSet", "completeFinalSet", "deletePlanningSet", "updateCompletedRecord", "deleteCompletedRecord"].includes(action);',
)
replace_once(
    "app/api/planning-lifecycle/route.ts",
    'function isObjectWithRecordId(input: unknown): input is { recordId: string } {',
    '''function isPlanningSetConflictPreviewInput(input: unknown): input is { setId: string; serviceDate: string; rows: PlanningSet["rows"] } {\n  return isRecord(input)\n    && typeof input.setId === "string"\n    && typeof input.serviceDate === "string"\n    && Array.isArray(input.rows)\n    && input.rows.every(isRecord);\n}\n\nfunction isObjectWithRecordId(input: unknown): input is { recordId: string } {''',
)

# 4) Client: race-safe authoritative preview + immediate optimistic result on changed candidate.
replace_once(
    "app/planning-lifecycle-client.tsx",
    'import { CandidateCombobox } from "../src/planning-lifecycle/candidate-list";',
    'import { CandidateCombobox } from "../src/planning-lifecycle/candidate-list";\nimport { resolvePlanningDraftConflictRow, type PlanningDraftConflictPreviewState } from "../src/planning-lifecycle/conflict-ui";',
)
replace_once(
    "app/planning-lifecycle-client.tsx",
    '''  async previewCompletedRecordInvalidation(input: { role: PlanningRole; localActorUserId: string; recordId: string; serviceContext: ServiceContext; set: PlanningSet & { status: "final" } }) {\n    return callPlanningLifecycleApi("previewCompletedRecordInvalidation", input, actorContextFrom(input));\n  }\n\n  async saveWorkingSet''',
    '''  async previewCompletedRecordInvalidation(input: { role: PlanningRole; localActorUserId: string; recordId: string; serviceContext: ServiceContext; set: PlanningSet & { status: "final" } }) {\n    return callPlanningLifecycleApi("previewCompletedRecordInvalidation", input, actorContextFrom(input));\n  }\n\n  async previewPlanningSetConflict(input: { setId: PlanningSetId; serviceDate: string; rows: PlanningRow[] }) {\n    return callPlanningLifecycleApi("previewPlanningSetConflict", input);\n  }\n\n  async saveWorkingSet''',
)
replace_once(
    "app/planning-lifecycle-client.tsx",
    '''  const [completedInvalidationPreview, setCompletedInvalidationPreview] = useState<CompletedPlanInvalidationPreview | null>(null);\n  const completedInvalidationPreviewRequest = useRef(0);\n  const [savedDbSets, setSavedDbSets] = useState<PersistedPlanningSet[]>([]);''',
    '''  const [completedInvalidationPreview, setCompletedInvalidationPreview] = useState<CompletedPlanInvalidationPreview | null>(null);\n  const completedInvalidationPreviewRequest = useRef(0);\n  const [planningDraftConflictPreview, setPlanningDraftConflictPreview] = useState<PlanningDraftConflictPreviewState | null>(null);\n  const planningDraftConflictPreviewRequest = useRef(0);\n  const [savedDbSets, setSavedDbSets] = useState<PersistedPlanningSet[]>([]);''',
)
replace_once(
    "app/planning-lifecycle-client.tsx",
    '''  const conflictingRevisionRowIndexes = useMemo(() => new Set(persistedSet?.needsRevision?.conflictingRowIndexes ?? []), [persistedSet?.id, persistedSet?.needsRevision]);\n  const completedConflictImpacts = completedInvalidationPreview?.impactedPlans ?? [];''',
    '''  const conflictingRevisionRowIndexes = useMemo(() => new Set(persistedSet?.needsRevision?.conflictingRowIndexes ?? []), [persistedSet?.id, persistedSet?.needsRevision]);\n  const planningConflictPreviewKey = useMemo(() => (\n    persistedSet && persistedSet.status === "working" && !completedRecord\n      ? JSON.stringify([persistedSet.id, serviceDate, planningRows.map((row) => row.song?.songId ?? null)])\n      : ""\n  ), [persistedSet?.id, persistedSet?.status, completedRecord?.id, serviceDate, planningRows]);\n  const completedConflictImpacts = completedInvalidationPreview?.impactedPlans ?? [];''',
)
effect_marker = '''  useEffect(() => {\n    const request = ++completedInvalidationPreviewRequest.current;\n    if (runtimeMode !== "db" || selectedRole !== "admin" || !completedRecord || !(planningLifecycleService instanceof DbPlanningLifecycleClient)) {'''
effect_insert = '''  useEffect(() => {\n    const request = ++planningDraftConflictPreviewRequest.current;\n    const previewKey = planningConflictPreviewKey;\n    if (runtimeMode !== "db" || !previewKey || !persistedSet || persistedSet.status !== "working" || completedRecord || !(planningLifecycleService instanceof DbPlanningLifecycleClient)) {\n      setPlanningDraftConflictPreview(null);\n      return;\n    }\n    void planningLifecycleService.previewPlanningSetConflict({\n      setId: persistedSet.id,\n      serviceDate,\n      rows: planningRows,\n    }).then((result) => {\n      if (request !== planningDraftConflictPreviewRequest.current) return;\n      if (!result.success) {\n        setPlanningDraftConflictPreview(null);\n        return;\n      }\n      const value = result.value as { conflictingRowIndexes?: number[] };\n      setPlanningDraftConflictPreview({ key: previewKey, conflictingRowIndexes: value.conflictingRowIndexes ?? [] });\n    }).catch(() => {\n      if (request === planningDraftConflictPreviewRequest.current) setPlanningDraftConflictPreview(null);\n    });\n    return () => {\n      if (planningDraftConflictPreviewRequest.current === request) planningDraftConflictPreviewRequest.current += 1;\n    };\n  }, [runtimeMode, planningConflictPreviewKey, persistedSet?.id, persistedSet?.status, completedRecord?.id, serviceDate, planningLifecycleService]);\n\n  useEffect(() => {\n    const request = ++completedInvalidationPreviewRequest.current;\n    if (runtimeMode !== "db" || selectedRole !== "admin" || !completedRecord || !(planningLifecycleService instanceof DbPlanningLifecycleClient)) {'''
replace_once("app/planning-lifecycle-client.tsx", effect_marker, effect_insert)
replace_once(
    "app/planning-lifecycle-client.tsx",
    '''            {rows.map((row, index) => {\n              const revisionConflict = Boolean((persistedSet?.needsRevision && conflictingRevisionRowIndexes.has(index)) || (completedRecord && completedConflictRowIndexes.has(index)));\n              return (''',
    '''            {rows.map((row, index) => {\n              const planningRevisionConflict = persistedSet && !completedRecord\n                ? resolvePlanningDraftConflictRow({\n                    persistedConflict: Boolean(persistedSet.needsRevision && conflictingRevisionRowIndexes.has(index)),\n                    persistedSongId: persistedSet.rows[index]?.song?.songId,\n                    draftSongId: planningRows[index]?.song?.songId,\n                    selectedCandidateSuppressedByMelodyWindow: row.selectedCandidate?.suppressedByMelodyWindow,\n                    currentPreviewKey: planningConflictPreviewKey,\n                    preview: planningDraftConflictPreview,\n                    rowIndex: index,\n                  })\n                : false;\n              const revisionConflict = Boolean(planningRevisionConflict || (completedRecord && completedConflictRowIndexes.has(index)));\n              return (''',
)

# 5) Update #230 regression for single-border semantics and add authoritative Working-preview checks.
replace_once(
    "scripts/issue-230-tests.ts",
    '''  assert.match(recordAlarmRule, /outline:\\s*3px solid var\\(--danger\\)/);\n  assert.match(recordAlarmRule, /background:\\s*#fef3f2/);\n  const rowAlarmRule = cssSource.match(/\\.needs-revision-row\\s*\\{([\\s\\S]*?)\\}/)?.[1] ?? "";\n  assert.match(rowAlarmRule, /outline:\\s*3px solid var\\(--danger\\)/);\n  assert.match(rowAlarmRule, /background:\\s*#fef3f2/);''',
    '''  assert.match(recordAlarmRule, /border:\\s*3px solid var\\(--danger\\)/);\n  assert.doesNotMatch(recordAlarmRule, /outline:/, "conflicting record must have one red contour, not border plus outline");\n  assert.match(recordAlarmRule, /background:\\s*#fef3f2/);\n  const rowAlarmRule = cssSource.match(/\\.needs-revision-row\\s*\\{([\\s\\S]*?)\\}/)?.[1] ?? "";\n  assert.match(rowAlarmRule, /border:\\s*3px solid var\\(--danger\\)/);\n  assert.doesNotMatch(rowAlarmRule, /outline:/, "conflicting fieldset must preserve native legend-gap geometry with one border");\n  assert.match(rowAlarmRule, /background:\\s*#fef3f2/);''',
)
preview_test_marker = '''    const loadedResponse = await planningLifecyclePost(requestFor("loadCompletedRecord", { recordId: completedId }));'''
preview_test_insert = '''    const workingConflictPreviewResponse = await planningLifecyclePost(requestFor("previewPlanningSetConflict", {\n      setId: working.id,\n      serviceDate: working.serviceContext.serviceDate,\n      rows: working.rows,\n    }));\n    assert.equal(workingConflictPreviewResponse.status, 200);\n    const workingConflictPreview = await workingConflictPreviewResponse.json() as { success: boolean; value?: PlanningPreviewValue };\n    assert.equal(workingConflictPreview.success, true);\n    assert.deepEqual(workingConflictPreview.value?.conflictingRowIndexes, [0], "current Working draft preview must expose the persisted historical conflict");\n\n    const resolvedWorkingPreviewResponse = await planningLifecyclePost(requestFor("previewPlanningSetConflict", {\n      setId: working.id,\n      serviceDate: working.serviceContext.serviceDate,\n      rows: [{ song: song(0) }, { song: song(4) }],\n    }));\n    assert.equal(resolvedWorkingPreviewResponse.status, 200);\n    const resolvedWorkingPreview = await resolvedWorkingPreviewResponse.json() as { success: boolean; value?: PlanningPreviewValue };\n    assert.equal(resolvedWorkingPreview.success, true);\n    assert.deepEqual(resolvedWorkingPreview.value?.conflictingRowIndexes, [], "replacing the conflicting Working song must clear the authoritative draft conflict before Save");\n\n    const loadedResponse = await planningLifecyclePost(requestFor("loadCompletedRecord", { recordId: completedId }));'''
replace_once("scripts/issue-230-tests.ts", preview_test_marker, preview_test_insert)
replace_once(
    "scripts/issue-230-tests.ts",
    '''type PreviewValue = {\n  impactedPlans: PreviewImpact[];\n  newlyImpactedPlans: PreviewImpact[];\n};''',
    '''type PreviewValue = {\n  impactedPlans: PreviewImpact[];\n  newlyImpactedPlans: PreviewImpact[];\n};\ntype PlanningPreviewValue = {\n  conflictingRowIndexes: number[];\n};''',
)

# 6) Dedicated Issue #233 pure UI acceptance.
Path("scripts/issue-233-tests.ts").write_text('''import assert from "node:assert/strict";\nimport { readFile } from "node:fs/promises";\nimport { resolvePlanningDraftConflictRow } from "../src/planning-lifecycle/conflict-ui";\n\nasync function main() {\n  const cssSource = await readFile("app/globals.css", "utf8");\n  const clientSource = await readFile("app/planning-lifecycle-client.tsx", "utf8");\n  const routeSource = await readFile("app/api/planning-lifecycle/route.ts", "utf8");\n\n  const rowAlarmRule = cssSource.match(/\\.needs-revision-row\\s*\\{([\\s\\S]*?)\\}/)?.[1] ?? "";\n  assert.match(rowAlarmRule, /border:\\s*3px solid var\\(--danger\\)/);\n  assert.doesNotMatch(rowAlarmRule, /outline:/);\n  const recordAlarmRule = cssSource.match(/\\.saved-set-list button\\.needs-revision-record\\s*\\{([\\s\\S]*?)\\}/)?.[1] ?? "";\n  assert.match(recordAlarmRule, /border:\\s*3px solid var\\(--danger\\)/);\n  assert.doesNotMatch(recordAlarmRule, /outline:/);\n\n  assert.match(clientSource, /previewPlanningSetConflict/);\n  assert.match(clientSource, /planningDraftConflictPreviewRequest/);\n  assert.match(clientSource, /resolvePlanningDraftConflictRow/);\n  assert.match(routeSource, /findCompletedPlanConflicts/);\n  assert.match(routeSource, /previewPlanningSetConflict/);\n\n  const persistedConflict = {\n    persistedConflict: true,\n    persistedSongId: "song:conflict",\n    currentPreviewKey: "new-key",\n    rowIndex: 0,\n  } as const;\n\n  assert.equal(resolvePlanningDraftConflictRow({\n    ...persistedConflict,\n    draftSongId: "song:conflict",\n    selectedCandidateSuppressedByMelodyWindow: true,\n    preview: null,\n  }), true, "unchanged conflicted Working row keeps the persisted alarm while preview is pending");\n\n  assert.equal(resolvePlanningDraftConflictRow({\n    ...persistedConflict,\n    draftSongId: "song:safe",\n    selectedCandidateSuppressedByMelodyWindow: false,\n    preview: { key: "old-key", conflictingRowIndexes: [0] },\n  }), false, "changed non-conflicting candidate clears the alarm immediately and ignores stale preview state");\n\n  assert.equal(resolvePlanningDraftConflictRow({\n    ...persistedConflict,\n    draftSongId: "song:other-conflict",\n    selectedCandidateSuppressedByMelodyWindow: true,\n    preview: { key: "old-key", conflictingRowIndexes: [] },\n  }), true, "changed conflicting candidate keeps the alarm immediately while authoritative preview is pending");\n\n  assert.equal(resolvePlanningDraftConflictRow({\n    ...persistedConflict,\n    draftSongId: "song:safe",\n    selectedCandidateSuppressedByMelodyWindow: false,\n    preview: { key: "new-key", conflictingRowIndexes: [] },\n  }), false, "current authoritative preview confirms the cleared conflict");\n\n  assert.equal(resolvePlanningDraftConflictRow({\n    ...persistedConflict,\n    draftSongId: "song:safe",\n    selectedCandidateSuppressedByMelodyWindow: false,\n    preview: { key: "new-key", conflictingRowIndexes: [0] },\n  }), true, "current authoritative preview overrides optimistic state when DB truth still conflicts");\n\n  console.log("Issue #233 alarm geometry and live Working conflict acceptance passed.");\n}\n\nvoid main().catch((error: unknown) => {\n  console.error(error);\n  process.exitCode = 1;\n});\n''', encoding="utf-8")

# 7) Dedicated PR workflow.
Path(".github/workflows/issue-233.yml").write_text('''name: Issue 233 live Working conflict acceptance\n\non:\n  pull_request:\n    paths:\n      - app/api/planning-lifecycle/route.ts\n      - app/globals.css\n      - app/planning-lifecycle-client.tsx\n      - src/planning-lifecycle/conflict-ui.ts\n      - scripts/issue-230-tests.ts\n      - scripts/issue-233-tests.ts\n      - .github/workflows/issue-233.yml\n  workflow_dispatch:\n\njobs:\n  issue-233:\n    runs-on: ubuntu-latest\n    services:\n      postgres:\n        image: postgres:16\n        env:\n          POSTGRES_USER: postgres\n          POSTGRES_PASSWORD: postgres\n          POSTGRES_DB: organy_issue_233\n        ports:\n          - 5432:5432\n        options: >-\n          --health-cmd="pg_isready -U postgres -d organy_issue_233"\n          --health-interval=10s\n          --health-timeout=5s\n          --health-retries=5\n    env:\n      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/organy_issue_233\n      ORGANY_RUNTIME: db\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 22\n          cache: npm\n      - run: npm ci\n      - run: npm run db:migrate\n      - name: Issue 230 regression\n        run: npx tsx scripts/issue-230-tests.ts\n      - name: Issue 233 UI state acceptance\n        run: npx tsx scripts/issue-233-tests.ts\n      - run: npm run typecheck\n      - run: npm test\n''', encoding="utf-8")
